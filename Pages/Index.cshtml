@page
@using Models
@model IndexModel
@{
    ViewData["Title"] = "Home page";
    var posts = ViewData["posts"] as List<Article>;
}

<ol>
    @foreach (var post in posts)        
    {
        <li>@post.Title</li>
    }
</ol>

